const renderUserRouter = () => {
    const userRouters = [
        {
            path: ROU
        }
    ]
}

const RouterCustom = () => {
    return renderUserRouter();
}

export default RouterCustom;